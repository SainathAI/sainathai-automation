name: Actions Diagnosis Test
on:
  push:
    branches: [ main ] # Must match your default branch name (main or master)

jobs:
  check_if_running:
    runs-on: ubuntu-latest
    steps:
      - name: Confirmation Step
        run: echo "🎉 Workflow file is running successfully!"
