name: Trigger Jules Session
on:
  issues:
    types: [labeled]
  # 💡 NEW LINE: Adds a manual trigger button
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to process'
        required: true
        default: '1'
